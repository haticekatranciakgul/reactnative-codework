import { StyleSheet } from "react-native";

export default StyleSheet.create({

    card: {
        backgroundColor: '#fff',
        padding: 16,
        margin: 8,
        borderRadius: 8,
        elevation: 2,
    },
    title: { fontSize: 18, fontWeight: 'bold' },
    location: { color: '#fff', backgroundColor: '#e53935', padding: 5, borderRadius: 10 },
    level: { color: '#e53935', fontWeight: 'bold', textAlign: 'right' }
 

})